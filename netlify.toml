[build]
  command = "node scripts/with-database-url.cjs npm run db:generate && node scripts/with-database-url.cjs --skip-if-missing npm run db:deploy && npm run build -w packages/shared && npm run build -w apps/api && npm run build -w apps/web"
  publish = "apps/web/dist"
  functions = "netlify/functions"

[build.environment]
  NODE_VERSION = "22"

[functions]
  node_bundler = "esbuild"
  external_node_modules = [
    "@nestjs/common",
    "@nestjs/config",
    "@nestjs/core",
    "@nestjs/jwt",
    "@nestjs/passport",
    "@nestjs/platform-express",
    "@prisma/client",
    "bcryptjs",
    "class-transformer",
    "class-validator",
    "express",
    "express-rate-limit",
    "helmet",
    "express-rate-limit",
    "helmet",
    "passport",
    "reflect-metadata",
    "rxjs",
    "web-push",
    "serverless-http"
  ]
  included_files = [
    "apps/api/dist/**",
    "apps/api/prisma/**",
    "node_modules/.prisma/**"
  ]

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/api/:splat"
  status = 200
  force = true

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
